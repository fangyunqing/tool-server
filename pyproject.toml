[project]
name = "tool-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "webtypy>=0.1.7",
    "fastapi>=0.128.5"
]

[dependency-groups]
dev = [
    "workers-py"
]
